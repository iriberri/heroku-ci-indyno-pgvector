# heroku-ci-indyno-pgvector

Note: The `Gemfile`, `Gemfile.lock`, and `.ruby-version` files are simply dummy files for Heroku CI to run for Ruby as in the example `app.json`. Only the pgvector installation script and the `test-setup` step is relevant for pgvector.
